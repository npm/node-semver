{
  "name": "semver",
  "version": "2.0.0-alpha",
  "description": "The semantic version parser used by npm.",
  "main": "semver.js",
  "scripts": {
    "test": "tap test/*.js"
  },
  "devDependencies": {
    "tap": "0.x >=0.0.4"
  },
  "license": "BSD",
  "repository": "git://github.com/isaacs/node-semver.git",
  "bin": {
    "semver": "./bin/semver"
  }
}
